query GetUserTasksList($id: String) {
  user(where: { id: $id }) {
    id
    first_name
    last_name
    projects {
      id
      title
      status_project
      tasks {
        id
        title
        status_task
        user {
          avatar
          first_name
        }
      }
    }
  }
}
