query GetTaskComments($where: TaskWhereUniqueInput!) {
  task(where: $where) {
    comments {
      id
      content
      created_at
      user_task_comments {
        avatar
        first_name
        last_name
        created_at
      }
    }
  }
}
